- if @order.open?
  %span.label.label-success
    This order is still open and accepting items.
- elsif @order.closed?
  %span.label.label-important
    This order is still closed and not accepting items.
